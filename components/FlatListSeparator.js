import * as React from 'react';
import {View} from 'react-native';

const FlatListItemSeparator= (props) =>
  <View style={{height: 1, width: '100%', backgroundColor: '#FB671E'}}/>

export default FlatListItemSeparator

